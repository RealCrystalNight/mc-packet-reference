# C00PacketKeepAlive — mined corpus (2 clients)

Ground truth for C00PacketKeepAlive. Modules listed per client with file paths and
grep context. cat the full file for verbatim code.


## November Recode 2.0

### scripting/wrapper/event/LuaEventPacket.java
```java
   38 |     packetIds.put(C00PacketKeepAlive.class, 0x00);
```


## LiquidSense Dev

### me/AquaVit/liquidSense/modules/misc/Disabler.java
```java
   14 | import net.minecraft.network.play.client.C00PacketKeepAlive;
   32 |     private final Queue<C00PacketKeepAlive> c00List = new LinkedList<>();
   79 |             C00PacketKeepAlive packetKeepAlive = new C00PacketKeepAlive(keepAliveId++);
   97 |                 C00PacketKeepAlive packet = c00List.peek();
  114 |         if (e.getPacket() instanceof C00PacketKeepAlive && !c00List.contains(e.getPacket())) {
  115 |             c00List.add((C00PacketKeepAlive) e.getPacket());
```

### net/ccbluex/liquidbounce/features/module/modules/exploit/PingSpoof.java
```java
   19 | import net.minecraft.network.play.client.C00PacketKeepAlive;
   63 | 		if ((packet instanceof C0FPacketConfirmTransaction || packet instanceof C00PacketKeepAlive) && !(mc.thePlayer.isDead || mc.thePlayer.getHealth() <= 0) && !packetsMap.containsKey(packet)) {
```

### net/ccbluex/liquidbounce/features/module/modules/movement/Fly.java
```java
   24 | import net.minecraft.network.play.client.C00PacketKeepAlive;
  423 |                 mc.getNetHandler().addToSendQueue(new C00PacketKeepAlive());
```
