# C00PacketLoginStart — mined corpus (2 clients)

Ground truth for C00PacketLoginStart. Modules listed per client with file paths and
grep context. cat the full file for verbatim code.


## LiquidSense Dev

### net/ccbluex/liquidbounce/injection/forge/mixins/gui/MixinGuiConnecting.java
```java
   23 | import net.minecraft.network.login.client.C00PacketLoginStart;
   67 |     @Inject(method = "connect", at = @At(value = "NEW", target = "net/minecraft/network/login/client/C00PacketLoginStart"), cancellable = true)
   70 | //            networkManager.sendPacket(new C00PacketLoginStart(new GameProfile(null, MCLeaks.getSession().getUsername())));
   94 |                 networkManager.sendPacket(new C00PacketLoginStart(MCLeaks.isAltActive() ? new GameProfile(null, MCLeaks.getSession().getUsername()) : mc.getSession().getProfile()));
```
