# S0BPacketAnimation — mined corpus (2 clients)

Ground truth for S0BPacketAnimation. Modules listed per client with file paths and
grep context. cat the full file for verbatim code.


## LiquidSense Dev

### net/ccbluex/liquidbounce/features/module/modules/misc/AntiBot.java
```java
   32 | import net.minecraft.network.play.server.S0BPacketAnimation;
  276 |         if (packet instanceof S0BPacketAnimation) {
  277 |             final S0BPacketAnimation packetAnimation = (S0BPacketAnimation) event.getPacket();
```
