{
  "overview": "The status query is a zero-payload packet that arrives before login, so no anticheat check touches it. The corpus contains only declarations: FlopAC NMSObject and Packet define the START constant for PacketStatusInStart, and Artemis wraps it as PacketStatusClientStart in its packet library. Server-list scanners are the only abuse surface, and that is a proxy-level concern, not a check domain.",
  "checks": []
}
