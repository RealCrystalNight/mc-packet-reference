{
  "overview": "The login encryption challenge is the least instrumented packet in this set: exactly one anticheat references it, and only as packet-library plumbing. Artemis maps SERVER_LOGIN + 2 to PacketLoginOutEncryptionBegin so login-state traffic is typed; no anticheat validates the RSA handshake, because the exchange is identical for every legitimate client and predates world state.",
  "checks": [
    {
      "name": "Login packet registration (Artemis)",
      "found_in": [
        "artemisac-artemis-minecraft-anticheat"
      ],
      "purpose": "Registers the encryption-request packet in Artemis's packet repository so the login-state stream is typed and readable.",
      "how_it_works": "PacketRepository puts SERVER_LOGIN + 2 at PacketLoginOutEncryptionBegin.class and the PacketLoginOutEncryptionBegin interface extends PacketServer. The interface is the full extent of Artemis's interest: no wrapper class and no check subscribes to the packet.",
      "detects": "None. This is registry plumbing for a packet that arrives before any check-relevant state exists.",
      "detailed_code": "// ===== FILE: artemisac-artemis-minecraft-anticheat — ac.artemis.packet.api/src/main/java/ac/artemis/packet/wrapper/server/PacketLoginOutEncryptionBegin.java =====\npackage ac.artemis.packet.wrapper.server;\n\nimport ac.artemis.packet.wrapper.PacketServer;\n\npublic interface PacketLoginOutEncryptionBegin extends PacketServer {\n}\n\n// NOTE (AI): This 5-line interface is the only corpus artifact for the encryption request. The repository mapping lives in PacketRepository.java at SERVER_LOGIN + 2.",
      "code_source": "artemisac-artemis-minecraft-anticheat — ac.artemis.packet.api/src/main/java/ac/artemis/packet/wrapper/server/PacketLoginOutEncryptionBegin.java",
      "variations": "The other 49 anticheats in the corpus never reference the packet by name or by PacketLoginOutEncryptionBegin. Login-state traffic is outside every check domain."
    }
  ]
}
