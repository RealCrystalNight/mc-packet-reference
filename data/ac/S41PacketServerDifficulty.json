{
  "overview": "The difficulty announcement is invisible to the anticheat corpus. The single hit is Artemis HardcodeMap, which maps PacketPlayOutServerDifficulty to its wrapper class for the packet generator (line 60). One unsigned byte sent once at join with no serverbound counterpart, so there is nothing to validate, spoof, or time; the value only scales vanilla survival math that server-side checks never see.",
  "checks": []
}
