{
  "writeup": [
    {
      "h": "Role in the protocol",
      "p": "Server-initiated window close. It carries a single unsigned byte windowId and tells the client to tear down the open container and re-sync the held item. Server-side, EntityPlayerMP.closeScreen sends it when the server decides the player's window must close, which happens on death, teleport, dimension change, and anti-cheat interventions. The client counterpart C0DPacketCloseWindow is the player-initiated echo; the S2E path is always server-driven."
    },
    {
      "h": "Vanilla handling",
      "p": "NetHandlerPlayClient.handleCloseWindow calls thePlayer.func_175159_q(), which resets the carried item and closes the current screen. It does not check the windowId against the open container: any S2E closes whatever GUI is up. Server-side, EntityPlayerMP.closeScreen sends S2E with openContainer.windowId and closes the container. The asymmetry is the hook: a server can yank any GUI, and the client obeys blindly."
    },
    {
      "h": "Why clients touch it",
      "p": "Two module families. NoGuiClose in Rise 5.99, 6.1.30 and 6.2.4 cancels S2E so servers cannot close the player's GUI; Rise 5.99 defaults the Chat Only setting on, meaning the cancel only applies while the chat screen is open, which preserves the ability to type while a container stays open. Rise 6.2.4 defaults it off, blocking every server close. LiquidSense Dev's KeepContainer uses S2E in the opposite direction: it reads the server's close to drop its cached container reference, and it cancels the client's own C0D close packets while the keep is armed. The other six clients (Memeware, Nekoware, Sigma, Spicy, Tenacity, November Recode 2.0) have no S2E module reference at all."
    },
    {
      "h": "Exploit surface",
      "p": "The unguarded close is the surface: servers close windows to interrupt chest-stealing macros and inventory manipulation, and NoGuiClose defeats that by holding the GUI open. The cost is protocol desync: the server has closed its container while the client still shows it, so later clicks (C0E) reference a dead window. This is safe only while the player does not actually interact; the Chat Only mode exists precisely to limit the desync window to chat-while-looting use. Vanilla's failure to validate windowId on S2E also means a server can send a mismatched id to force-close anything."
    }
  ],
  "overview": "The server-driven window close. Rise ships NoGuiClose in all three releases to cancel S2E and hold the GUI open, with a Chat Only mode in 5.99 to limit the desync. LiquidSense Dev's KeepContainer listens to S2E to invalidate its cached container when the server closes it. The other six clients never touch the packet outside vanilla.",
  "server_handling": "EntityPlayerMP.closeScreen sends S2E with the current openContainer.windowId, then closes the server-side container and re-syncs the held item. NetHandlerPlayClient.handleCloseWindow runs func_175159_q() unconditionally: it resets the carried item and closes whatever screen is active, ignoring the packet's windowId entirely. Because the client never validates the id, the packet is a blind close lever from the server's perspective.",
  "protocol_notes": "Encoding: single unsigned byte windowId. It is one of the smallest packets in the play state. The client handler performs no id comparison against openContainer.windowId, which is the load-bearing detail for NoGuiClose: cancelling the packet has no side effects beyond keeping the GUI open. The windowId matters only for server bookkeeping, since the client close path is unconditional.",
  "anticheat_landscape": "Anti-cheats flag the desync NoGuiClose creates: a window the server closed but the client keeps open will eventually produce clicks (C0E) against a dead windowId, which window-state checks flag as inventory manipulation. The Chat Only default in Rise 5.99 keeps the desync window small (chat open while looting); blocking every S2E (6.2.4 default) widens it and increases the flag surface. This is a utility module with a real anti-cheat cost, unlike the purely cosmetic filters elsewhere in this group.",
  "modules": [
    {
      "name": "NoGuiClose",
      "found_in": [
        "Rise 5.99",
        "Rise 6.2.4",
        "Rise 6.1.30"
      ],
      "purpose": "Cancel server window-close packets so the player's GUI cannot be yanked away.",
      "how_it_works": "A PacketReceiveEvent handler cancels any S2E, gated by a Chat Only setting: when enabled, the cancel only fires while mc.currentScreen is a GuiChat. Rise 5.99 defaults Chat Only to true (onPacketReceive + event.setCancelled), Rise 6.2.4 defaults its chatonly value to false (Listener with event.setCancelled).",
      "detailed_code": "// ===== FILE: Rise 5.99 — dev/rise/module/impl/other/NoGuiClose.java =====\n/*\n Copyright Alan Wood 2021\n None of this code to be reused without my written permission\n Intellectual Rights owned by Alan Wood\n */\npackage dev.rise.module.impl.other;\n\nimport dev.rise.event.impl.packet.PacketReceiveEvent;\nimport dev.rise.module.Module;\nimport dev.rise.module.api.ModuleInfo;\nimport dev.rise.module.enums.Category;\nimport dev.rise.setting.impl.BooleanSetting;\nimport net.minecraft.client.gui.GuiChat;\nimport net.minecraft.network.play.server.S2EPacketCloseWindow;\n\n@ModuleInfo(name = \"NoGuiClose\", description = \"Prevents servers from closing opened Guis\", category = Category.OTHER)\npublic final class NoGuiClose extends Module {\n\n    private final BooleanSetting chatOnly = new BooleanSetting(\"Chat Only\", this, true);\n\n    @Override\n    public void onPacketReceive(final PacketReceiveEvent event) {\n        if (event.getPacket() instanceof S2EPacketCloseWindow && (mc.currentScreen instanceof GuiChat || !chatOnly.isEnabled()))\n            event.setCancelled(true);\n    }\n}\n\n// NOTE (AI): The whole module is the single onPacketReceive guard. The chatOnly setting defaults to true in 5.99, limiting the cancel to chat-open moments; 6.2.4 inverts the default to false.",
      "code_source": "Rise 5.99 — dev/rise/module/impl/other/NoGuiClose.java",
      "vanilla_hook": "PacketReceiveEvent (onPacketReceive override in 5.99; EventLink Listener in 6.x).",
      "anti_cheat_notes": "Clicks against a server-closed window are the flag surface. The chat-gated variant keeps the desync brief; the unconditional variant trades safety for convenience and is the one most likely to trip window-state checks on servers that close windows for inventory-macro protection.",
      "variations": "Rise 6.1.30 and 6.2.4 ship identical files (BooleanValue chatonly, default false). Rise 5.99 uses BooleanSetting chatOnly, default true, and the older onPacketReceive override style."
    },
    {
      "name": "KeepContainer",
      "found_in": [
        "LiquidSense Dev"
      ],
      "purpose": "Keep a formerly opened inventory container accessible anywhere: re-open it with the INSERT key and suppress close packets while armed.",
      "how_it_works": "Stores the last non-inventory GuiContainer on ScreenEvent; pressing INSERT re-opens it via displayGuiScreen. On PacketEvent it cancels every C0DPacketCloseWindow and, for S2EPacketCloseWindow, clears the cached container when the packet's windowId matches the stored container, so the cache cannot be resurrected after the server closed it. Disabling the module sends the C0D close it had been suppressing.",
      "detailed_code": "// ===== FILE: LiquidSense Dev — net/ccbluex/liquidbounce/features/module/modules/exploit/KeepContainer.java =====\n/*\n * LiquidBounce Hacked Client\n * A free open source mixin-based injection hacked client for Minecraft using Minecraft Forge.\n * https://github.com/CCBlueX/LiquidBounce/\n */\npackage net.ccbluex.liquidbounce.features.module.modules.exploit;\n\nimport net.ccbluex.liquidbounce.event.EventTarget;\nimport net.ccbluex.liquidbounce.event.KeyEvent;\nimport net.ccbluex.liquidbounce.event.PacketEvent;\nimport net.ccbluex.liquidbounce.event.ScreenEvent;\nimport net.ccbluex.liquidbounce.features.module.Module;\nimport net.ccbluex.liquidbounce.features.module.ModuleCategory;\nimport net.ccbluex.liquidbounce.features.module.ModuleInfo;\nimport net.minecraft.client.gui.inventory.GuiContainer;\nimport net.minecraft.client.gui.inventory.GuiInventory;\nimport net.minecraft.network.play.client.C0DPacketCloseWindow;\nimport net.minecraft.network.play.server.S2EPacketCloseWindow;\nimport org.lwjgl.input.Keyboard;\n\n@ModuleInfo(name = \"KeepContainer\", description = \"Allows you to open a formerly closed inventory container everywhere. (Press INSERT Key to open)\", category = ModuleCategory.EXPLOIT)\npublic class KeepContainer extends Module {\n    private GuiContainer container;\n\n    @Override\n    public void onDisable() {\n        if(container != null)\n            mc.getNetHandler().addToSendQueue(new C0DPacketCloseWindow(container.inventorySlots.windowId));\n\n        container = null;\n    }\n\n    @EventTarget\n    public void onGui(final ScreenEvent event) {\n        if(event.getGuiScreen() instanceof GuiContainer && !(event.getGuiScreen() instanceof GuiInventory))\n            container = (GuiContainer) event.getGuiScreen();\n    }\n\n    @EventTarget\n    public void onKey(final KeyEvent event) {\n        if(event.getKey() == Keyboard.KEY_INSERT) {\n            if(container == null)\n                return;\n\n            mc.displayGuiScreen(container);\n        }\n    }\n\n    @EventTarget\n    public void onPacket(final PacketEvent event) {\n        if(event.getPacket() instanceof C0DPacketCloseWindow)\n            event.cancelEvent();\n\n        if(event.getPacket() instanceof S2EPacketCloseWindow) {\n            final S2EPacketCloseWindow packetCloseWindow = (S2EPacketCloseWindow) event.getPacket();\n\n            if(container != null && container.inventorySlots != null && packetCloseWindow.windowId == container.inventorySlots.windowId)\n                container = null;\n        }\n    }\n}\n// NOTE (AI): The S2E branch is defensive bookkeeping: it keeps the cached GuiContainer from outliving the server's close. The C0D cancel is the actual exploit surface, and the disable path flushes the suppressed close.",
      "code_source": "LiquidSense Dev — net/ccbluex/liquidbounce/features/module/modules/exploit/KeepContainer.java",
      "vanilla_hook": "LiquidBounce PacketEvent, ScreenEvent and KeyEvent.",
      "anti_cheat_notes": "Cancelling C0D while a window is open desyncs the client from the server's container state, the same flag surface NoGuiClose creates; the S2E listener bounds the desync by dropping the cached container as soon as the server closes it.",
      "variations": "Rise's NoGuiClose keeps the live GUI open and never re-opens a closed container; KeepContainer instead re-opens a snapshot of a container the server believes is closed. The S2E handling is bookkeeping rather than a cancel."
    }
  ],
  "general_hooks": "Two patterns: Rise cancels S2E on the packet receive path, optionally gated by chat-screen state; LiquidSense Dev's KeepContainer reads S2E to clear its cached container and cancels C0D close packets instead.",
  "client_variations": "Rise 5.99 uses the Module.onPacketReceive override and BooleanSetting; Rise 6.x uses EventLink Listener fields and BooleanValue. Both default the Chat Only gate differently (true vs false). Memeware, Nekoware, Sigma, Spicy, Tenacity and November Recode 2.0 reference S2E only in vanilla sources.",
  "related": [
    "S2DPacketOpenWindow",
    "S2FPacketSetSlot",
    "S30PacketWindowItems"
  ]
}
