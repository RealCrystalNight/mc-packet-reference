{
  "id": "S02PacketLoginSuccess",
  "hex": "0x02",
  "dec": 2,
  "dir": "CLIENTBOUND",
  "state": "LOGIN",
  "name": "Login Success",
  "desc": "Confirms successful authentication. Contains the player's UUID and username. Transitions the connection state from LOGIN to PLAY.",
  "tags": [
    "login",
    "authentication",
    "player"
  ],
  "fields": [
    {
      "name": "uuid",
      "type": "String (36 chars)",
      "desc": "Player's UUID in standard format (e.g. '123e4567-e89b-12d3-a456-426614174000')"
    },
    {
      "name": "username",
      "type": "String (max 16)",
      "desc": "The player's confirmed username"
    }
  ],
  "encoding": [
    [
      "UUID",
      "String",
      "UUID with dashes"
    ],
    [
      "Username",
      "String",
      "Player's Minecraft username"
    ]
  ],
  "mcp": [
    "NetHandlerLoginClient.handleLoginSuccess()"
  ],
  "handler": "INetHandlerLoginClient",
  "notes": "After receiving this, the client connection state transitions to PLAY. The client creates the EntityPlayerSP (local player entity) with the received UUID and username. Compression may be enabled next via S03PacketEnableCompression.",
  "implementation": {
    "writeup": [
      {
        "h": "Role in the protocol",
        "p": "The LOGIN-state success packet. NetHandlerLoginServer sends it after encryption and session validation complete: `new S02PacketLoginSuccess(this.loginGameProfile)`, carrying the confirmed UUID and username as two Strings. Vanilla client handling is the state transition itself: NetHandlerLoginClient.handleLoginSuccess stores the GameProfile, calls `setConnectionState(EnumConnectionState.PLAY)`, and installs the NetHandlerPlayClient over the same connection. From this instant the module system's world exists conceptually, but the world itself only materializes on S01PacketJoinGame."
      },
      {
        "h": "Vanilla handling",
        "p": "handleLoginSuccess is three lines in the 1.8.9 sources: `this.field_175091_e = packetIn.func_179730_a(); this.field_147393_d.setConnectionState(EnumConnectionState.PLAY); this.field_147393_d.setNetHandler(new NetHandlerPlayClient(this.field_147394_b, this.field_147395_c, this.field_147393_d, this.field_175091_e));`. The gameProfile field is the canonical UUID/name the client uses for the whole session; the session service may return a name different from the one sent in C00PacketLoginStart, and this packet is where the correction lands."
      },
      {
        "h": "Why clients touch it",
        "p": "The corpus has exactly one non-vanilla reference: Rise 5.99 StaffAlert imports S02PacketLoginSuccess at line 18, and the import is unused (the module's logic touches only S02PacketChat and the Hypixel API). No module in the 8 clients reads the UUID or name from this packet. The structural reason: the packet is consumed by the login handler before any module event bus is live, and clients that need their own UUID or username read them from mc.getSession() or their GameProfile, not from the network. Session-stealing or alt-login features that would care about this packet are absent from the corpus."
      },
      {
        "h": "Exploit surface",
        "p": "The profile data is server-asserted. An offline-mode server can send any UUID and username it likes, which is the entire basis of name-based alt accounts and cracked-server identity; clients that display 'authenticated' state off this packet would be trivially spoofed. On online-mode servers the values are Mojang-confirmed, so the packet is the client's first trusted identity. No timing or state abuse exists here: the transition to PLAY is instantaneous and any delay would only stall the loading screen."
      }
    ],
    "overview": "The LOGIN-state success packet that hands the client its confirmed UUID and username and flips the connection to PLAY. Pure vanilla in 7 of 8 clients; the only module hit is an unused import in Rise 5.99 StaffAlert. Nothing in the corpus reads the profile from this packet.",
    "server_handling": "NetHandlerLoginServer sends S02PacketLoginSuccess after the encryption response validates: the server decrypts the shared secret, verifies the token, joins the session server, then sets its own state to PLAY and sends the packet, optionally preceded by S03PacketEnableCompression when compression is configured. After the send, the server installs its NetHandlerPlayServer. The client treats the packet as authoritative for the session profile.",
    "protocol_notes": "Wire layout: String UUID (36 chars with dashes), then String username (max 16). Compression, when enabled, is negotiated in LOGIN state before this packet, so the first PLAY packet may already be compressed. Ordering: S02PacketLoginSuccess is the last LOGIN-state packet; the connection then expects S01PacketJoinGame as the first PLAY-state packet. The username here can differ from the login-start name when Mojang renames or corrects it.",
    "anticheat_landscape": "None. The packet precedes world state and carries only identity; anti-cheats are irrelevant to it. The security-relevant property is authentication strength: on online-mode servers the UUID is Mojang-signed, on offline servers it is arbitrary, and plugins that trust UUIDs for permissions inherit that trust boundary. No client module in the corpus touches it.",
    "modules": [
      {
        "name": "StaffAlert",
        "found_in": [
          "Rise 5.99"
        ],
        "purpose": "Hypixel staff-ban tracker. Its reference to this packet is an import only; the module does not use S02PacketLoginSuccess.",
        "how_it_works": "The corpus hit is line 18, `import net.minecraft.network.login.server.S02PacketLoginSuccess;`. The module's actual logic captures the Hypixel API key from S02PacketChat, sends /api new until the key is set, and polls api.hypixel.net/punishmentstats on a timer thread. The login-success import, like the S01PacketPong, C00Handshake and S40PacketDisconnect imports, is dead.",
        "detailed_code": null,
        "code_source": "Rise 5.99 — dev/rise/module/impl/other/StaffAlert.java",
        "vanilla_hook": "None for this packet; the module hooks PacketReceiveEvent for S02PacketChat and UpdateEvent for polling.",
        "anti_cheat_notes": "Irrelevant here: the import is never exercised. A login-success hook would have let the module reset its timer at session start; the author left the import but never wired it.",
        "variations": "No other client has any module reference to S02PacketLoginSuccess; the remaining corpus hits across all 8 clients are vanilla login plumbing."
      }
    ],
    "client_variations": "All 8 clients ship the stock handleLoginSuccess. Rise 6.x and Tenacity rename handler parameters to final; Spicy and Tenacity keep the same three-line body. None of the clients forks the login handler to emit module events.",
    "related": [
      "S01PacketEncryptionRequest",
      "C00PacketLoginStart",
      "S03PacketEnableCompression",
      "S01PacketJoinGame"
    ],
    "anticheat": {
      "overview": "The login success packet is another near-empty corpus: only Artemis references it, as packet-library plumbing. PacketRepository maps SERVER_LOGIN + 4 to PacketLoginOutSuccess and GPacketLoginServerSuccess wraps the wire format so login traffic is typed. No anticheat reads the UUID or username, because the packet carries only server-asserted identity and arrives before any check-relevant state exists.",
      "checks": [
        {
          "name": "Login success wrapper (Artemis)",
          "found_in": [
            "artemisac-artemis-minecraft-anticheat"
          ],
          "purpose": "Wraps PacketLoginOutSuccess so Artemis's packet library can read the login-success UUID and username from the wire.",
          "how_it_works": "GPacketLoginServerSuccess is linked to the PacketLoginOutSuccess interface and implements ReadableBuffer. Its read method handles both layouts: pre-1.16 reads a 36-char UUID string plus a 16-char name, and 1.16+ reads four ints that are reassembled into a UUID plus the name. The result is stored as a GameProfile.",
          "detects": "None. This is a typed read of server-asserted identity, not a check. No Artemis check subscribes to the packet.",
          "detailed_code": "// ===== FILE: artemisac-artemis-minecraft-anticheat — ac.artemis.packet.spigot/src/main/java/cc/ghast/packet/wrapper/packet/login/GPacketLoginServerSuccess.java =====\npackage cc.ghast.packet.wrapper.packet.login;\n\nimport ac.artemis.packet.protocol.ProtocolVersion;\nimport ac.artemis.packet.spigot.protocol.PacketLink;\nimport ac.artemis.packet.spigot.wrappers.GPacket;\nimport ac.artemis.packet.wrapper.server.PacketLoginOutSuccess;\nimport cc.ghast.packet.buffer.ProtocolByteBuf;\nimport cc.ghast.packet.wrapper.mc.GameProfile;\nimport cc.ghast.packet.wrapper.packet.ReadableBuffer;\n\nimport java.util.UUID;\n\n@PacketLink(PacketLoginOutSuccess.class)\npublic class GPacketLoginServerSuccess extends GPacket implements ReadableBuffer {\n    public GPacketLoginServerSuccess(UUID player, ProtocolVersion version) {\n        super(\"PacketLoginOutServerSuccess\", player, version);\n    }\n\n    private GameProfile gameProfile;\n\n    @Override\n    public void read(ProtocolByteBuf byteBuf) {\n        final UUID uuid;\n        final String username;\n\n        if (version.isOrAbove(ProtocolVersion.V1_16)) {\n            int[] aint = new int[4];\n\n            for(int i = 0; i < aint.length; ++i) {\n                aint[i] = byteBuf.readInt();\n            }\n\n            uuid = decodeUUID(aint);\n            username = byteBuf.readStringBuf(16);\n        } else {\n            final String uid = byteBuf.readStringBuf(36);\n            uuid = UUID.fromString(uid);\n            username = byteBuf.readStringBuf(16);\n        }\n\n        this.gameProfile = new GameProfile(uuid, username);\n    }\n\n    private static UUID decodeUUID(int[] bits) {\n        return new UUID((long)bits[0] << 32 | (long)bits[1] & 4294967295L, (long)bits[2] << 32 | (long)bits[3] & 4294967295L);\n    }\n\n    public GameProfile getGameProfile() {\n        return this.gameProfile;\n    }\n}\n\n// NOTE (AI): The wrapper is the only non-vanilla corpus artifact for the login success packet. The PacketRepository mapping sits at SERVER_LOGIN + 4.",
          "code_source": "artemisac-artemis-minecraft-anticheat — ac.artemis.packet.spigot/src/main/java/cc/ghast/packet/wrapper/packet/login/GPacketLoginServerSuccess.java",
          "variations": "The other 49 anticheats never reference the packet. Its only role in the corpus is keeping Artemis's login-state stream typed."
        }
      ]
    }
  }
}