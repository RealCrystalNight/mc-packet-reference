{
  "id": "S20PacketEntityProperties",
  "hex": "0x20",
  "dec": 32,
  "dir": "CLIENTBOUND",
  "state": "PLAY",
  "name": "Entity Properties",
  "desc": "Updates an entity's generic attributes (movement speed, max health, knockback resistance, etc.).",
  "tags": [
    "entity",
    "attributes",
    "speed"
  ],
  "fields": [
    {
      "name": "entityId",
      "type": "VarInt",
      "desc": "Entity ID"
    },
    {
      "name": "properties",
      "type": "List of Attribute",
      "desc": "Attribute key-value pairs"
    }
  ],
  "encoding": [
    [
      "Entity ID",
      "VarInt",
      ""
    ],
    [
      "Number of Properties",
      "int",
      ""
    ],
    [
      "Per-Property",
      "String + double + VarInt + Modifier[]",
      "Attribute key, base value, modifiers"
    ]
  ],
  "mcp": [
    "NetHandlerPlayClient.handleEntityProperties()"
  ],
  "handler": "INetHandlerPlayClient",
  "notes": "Attribute keys: generic.movementSpeed, generic.maxHealth, generic.knockbackResistance, generic.followRange, horse.jumpStrength, zombie.spawnReinforcements, etc. Movement speed can be read to predict horse speeds or detect speed-modified players.",
  "implementation": {
    "writeup": [
      {
        "h": "Role in the protocol",
        "p": "S20PacketEntityProperties is a full attribute snapshot for one entity: entityId VarInt, property count int, then per property a String key, double base value, modifier count, and per modifier a UUID, name, amount and operation byte. The server sends it from EntityTrackerEntry when an entity's attributes change (line 267/311 in the corpus) and to new watchers when they start tracking (line 329/374). This is the only channel by which the client learns other entities' movement speed, knockback resistance, max health and attack damage."
      },
      {
        "h": "Vanilla handling",
        "p": "NetHandlerPlayClient.handleEntityProperties resolves the entity and throws IllegalStateException if it is not EntityLivingBase ('Server tried to update attributes of a non-living entity'). It then walks the snapshot list: unknown attribute keys are registered on the fly as RangedAttribute with the full double range, setBaseValue is applied, all modifiers are removed and re-added from the wire. EntityLivingBase reads these attributes for its own speed, health and knockback math, so the packet directly drives client-side entity physics."
      },
      {
        "h": "Why clients touch it",
        "p": "Rise 6.1.30 and 6.2.4 PingSpoofComponent include S20PacketEntityProperties in the 'players' class tuple: when a module enables players=true spoofing, incoming S20 packets are cancelled and released late. Rise 6.x NetworkAPI also registers the class (clientbound index 75). Memeware, Nekoware, Sigma, Spicy and Tenacity carry only the vanilla handler; the AntiBot/TargetInfo attribute-reading modules from older docs have no corpus hits in the reference set."
      },
      {
        "h": "Exploit surface",
        "p": "The interesting abuse is buffering, not reading: PingSpoofComponent holds S20 for other entities so their attribute snapshots arrive after their position updates, keeping the client's view of entity speeds consistent with frozen positions during blink windows. Delaying entity-state packets does not touch the player's own server-authoritative motion, so no anti-cheat flags it. A client that reads S20 precisely could predict player speeds for aim/velocity math, but none of the 8 clients does."
      }
    ],
    "overview": "The client's only window into other entities' attributes. Rise 6.x PingSpoofComponent buffers S20 as part of its 'players' spoof category, and NetworkAPI registers it for scripts; the other six clients leave the vanilla handler untouched.",
    "server_handling": "EntityTrackerEntry sends the full attribute map on change (line 267/311) and on watcher start (line 329/374). The packet carries base values plus modifiers, so the client rebuilds the complete attribute state from scratch on every arrival; there is no delta encoding and no inbound handler. Server-side validation is absent by design: the packet is clientbound.",
    "protocol_notes": "The property count is a plain int, not a VarInt, unlike most 1.8.9 counts. Attribute keys are the unlocalized names ('generic.movementSpeed', 'generic.knockbackResistance', 'generic.maxHealth', 'generic.attackDamage', 'horse.jumpStrength', 'zombie.spawnReinforcements'). Modifier operation is a byte (0 add, 1 multiply, 2 multiply-base). The client registers unknown keys as RangedAttribute with near-infinite bounds, so a server can push arbitrary named attributes.",
    "anticheat_landscape": "Entity attribute state is derived server-side, so anti-cheats never validate S20 traffic. The only observable effect of buffering it is delayed visual entity speed, which looks like normal lag. This is why PingSpoofComponent can park S20 for hundreds of milliseconds without risk.",
    "modules": [
      {
        "name": "PingSpoofComponent",
        "found_in": [
          "Rise 6.1.30",
          "Rise 6.2.4"
        ],
        "purpose": "Buffers S20PacketEntityProperties for other entities during a spoof/blink window so attribute snapshots arrive late and stay consistent with frozen entity positions.",
        "how_it_works": "The 'players' Tuple lists S20PacketEntityProperties alongside S13PacketDestroyEntities, S14PacketEntity variants, S18PacketEntityTeleport and S19PacketEntityHeadLook. onPacketReceive cancels any listed class when its category flag is enabled and parks it as a TimedPacket; onPostMotion flushes entries whose time plus the spoof amount has elapsed, and dispatch() releases everything on world change or disable.",
        "detailed_code": "// ===== FILE: Rise 6.1.30 — com/alan/clients/component/impl/player/PingSpoofComponent.java =====\npackage com.alan.clients.component.impl.player;\n\nimport com.alan.clients.component.Component;\nimport com.alan.clients.event.CancellableEvent;\nimport com.alan.clients.event.Listener;\nimport com.alan.clients.event.annotations.EventLink;\nimport com.alan.clients.event.impl.motion.PostMotionEvent;\nimport com.alan.clients.event.impl.other.WorldChangeEvent;\nimport com.alan.clients.event.impl.packet.PacketReceiveEvent;\nimport com.alan.clients.event.impl.packet.PacketSendEvent;\nimport com.alan.clients.util.packet.PacketUtil;\nimport net.minecraft.client.gui.GuiDownloadTerrain;\nimport net.minecraft.network.Packet;\nimport net.minecraft.network.play.client.*;\nimport net.minecraft.network.play.server.*;\nimport net.minecraft.util.Tuple;\nimport rip.vantage.commons.util.time.StopWatch;\n\nimport java.util.Arrays;\nimport java.util.concurrent.ConcurrentLinkedQueue;\n\npublic final class PingSpoofComponent extends Component {\n    public static ConcurrentLinkedQueue<PacketUtil.TimedPacket> packets = new ConcurrentLinkedQueue<>();\n    static StopWatch enabledTimer = new StopWatch();\n    public static boolean enabled;\n    static long amount;\n    static Tuple<Class[], Boolean> regular = new Tuple<>(new Class[]{C0FPacketConfirmTransaction.class, C00PacketKeepAlive.class, S1CPacketEntityMetadata.class}, false);\n    static Tuple<Class[], Boolean> velocity = new Tuple<>(new Class[]{S12PacketEntityVelocity.class, S27PacketExplosion.class}, false);\n    static Tuple<Class[], Boolean> teleports = new Tuple<>(new Class[]{S08PacketPlayerPosLook.class, S39PacketPlayerAbilities.class, S09PacketHeldItemChange.class}, false);\n    static Tuple<Class[], Boolean> players = new Tuple<>(new Class[]{S13PacketDestroyEntities.class, S14PacketEntity.class, S14PacketEntity.S16PacketEntityLook.class, S14PacketEntity.S15PacketEntityRelMove.class, S14PacketEntity.S17PacketEntityLookMove.class, S18PacketEntityTeleport.class, S20PacketEntityProperties.class, S19PacketEntityHeadLook.class}, false);\n    static Tuple<Class[], Boolean> blink = new Tuple<>(new Class[]{C02PacketUseEntity.class, C0DPacketCloseWindow.class, C0EPacketClickWindow.class, C0CPacketInput.class, C0BPacketEntityAction.class, C08PacketPlayerBlockPlacement.class, C07PacketPlayerDigging.class, C09PacketHeldItemChange.class, C13PacketPlayerAbilities.class, C15PacketClientSettings.class, C16PacketClientStatus.class, C17PacketCustomPayload.class, C18PacketSpectate.class, C19PacketResourcePackStatus.class, C03PacketPlayer.class, C03PacketPlayer.C04PacketPlayerPosition.class, C03PacketPlayer.C05PacketPlayerLook.class, C03PacketPlayer.C06PacketPlayerPosLook.class, C0APacketAnimation.class}, false);\n    static Tuple<Class[], Boolean> movement = new Tuple<>(new Class[]{C03PacketPlayer.class, C03PacketPlayer.C04PacketPlayerPosition.class, C03PacketPlayer.C05PacketPlayerLook.class, C03PacketPlayer.C06PacketPlayerPosLook.class}, false);\n\n    public static Tuple<Class[], Boolean>[] types = new Tuple[]{regular, velocity, teleports, players, blink, movement};\n\n    @EventLink\n    public final Listener<PacketSendEvent> send = event -> event.setCancelled(onPacket(event.getPacket(), event).isCancelled());\n\n    @EventLink\n    public final Listener<PacketReceiveEvent> receive = event -> event.setCancelled(onPacket(event.getPacket(), event).isCancelled());\n\n    public CancellableEvent onPacket(Packet<?> packet, CancellableEvent event) {\n        if (!event.isCancelled() && enabled && Arrays.stream(types).anyMatch(tuple -> tuple.getSecond() && Arrays.stream(tuple.getFirst()).anyMatch(regularpacket -> regularpacket == packet.getClass()))) {\n            event.setCancelled();\n            packets.add(new PacketUtil.TimedPacket(packet));\n        }\n\n        return event;\n    }\n\n    public static void dispatch() {\n        if (!packets.isEmpty()) {\n            // Stops the packets from being called twice\n            boolean enabled = PingSpoofComponent.enabled;\n            PingSpoofComponent.enabled = false;\n            packets.forEach(timedPacket -> PacketUtil.queue(timedPacket.getPacket()));\n            PingSpoofComponent.enabled = enabled;\n            packets.clear();\n        }\n    }\n\n    public static void disable() {\n        enabled = false;\n        enabledTimer.setMillis(enabledTimer.millis - 999999999);\n    }\n\n    @EventLink\n    public final Listener<WorldChangeEvent> onWorldChange = event -> dispatch();\n\n    @EventLink\n    public final Listener<PostMotionEvent> onPostMotion = event -> {\n        if (!(enabled = !enabledTimer.finished(100) && !(mc.currentScreen instanceof GuiDownloadTerrain))) {\n            dispatch();\n        } else {\n            // Stops the packets from being called twice\n            enabled = false;\n\n            packets.forEach(packet -> {\n                if (packet.getTime() + amount < System.currentTimeMillis()) {\n                    PacketUtil.queue(packet.getPacket());\n                    packets.remove(packet);\n                }\n            });\n\n            enabled = true;\n        }\n    };\n\n    public static void spoof(int amount, boolean regular, boolean velocity, boolean teleports, boolean players) {\n        spoof(amount, regular, velocity, teleports, players, false);\n    }\n\n    public static void spoof(int amount, boolean regular, boolean velocity, boolean teleports, boolean players, boolean blink, boolean movement) {\n        enabledTimer.reset();\n\n        PingSpoofComponent.regular.setSecond(regular);\n        PingSpoofComponent.velocity.setSecond(velocity);\n        PingSpoofComponent.teleports.setSecond(teleports);\n        PingSpoofComponent.players.setSecond(players);\n        PingSpoofComponent.blink.setSecond(blink);\n        PingSpoofComponent.movement.setSecond(movement);\n        PingSpoofComponent.amount = amount;\n    }\n\n    public static void spoof(int amount, boolean regular, boolean velocity, boolean teleports, boolean players, boolean blink) {\n        spoof(amount, regular, velocity, teleports, players, blink, false);\n    }\n\n    public static void blink() {\n        spoof(9999999, true, false, false, false, true);\n    }\n}\n// NOTE (AI): S20PacketEntityProperties appears on line 30 inside the 'players'\n// class tuple: when a module calls PingSpoofComponent.spoof(amount, ..., players=true),\n// S20 packets for other entities are cancelled on receive, parked as TimedPacket\n// entries, and flushed from onPostMotion once their delay elapses. The point is\n// to keep attribute snapshots (movement speed, knockback resistance, health)\n// consistent with the frozen entity positions during a blink/spoof window.",
        "code_source": "Rise 6.1.30 — com/alan/clients/component/impl/player/PingSpoofComponent.java",
        "vanilla_hook": "PacketReceiveEvent (cancel + park), PostMotionEvent (flush), WorldChangeEvent (dispatch), PacketSendEvent (serverbound park).",
        "anti_cheat_notes": "Low risk: only entity-state packets are delayed, never the player's own movement or transaction stream, so server-side motion checks see nothing unusual. Delayed entity attributes render as slightly stale entity speeds, which reads as latency.",
        "variations": "Rise 6.2.4 carries an identical file under Client-release2/Client/src/main/java. Rise 6.0 (outside the reference set) has the same component; within the reference set only 6.1.30 and 6.2.4 count."
      }
    ],
    "general_hooks": "Rise 6.x component system: @EventLink listeners on PacketReceiveEvent, PacketSendEvent, PostMotionEvent and WorldChangeEvent. Modules request spoofing via PingSpoofComponent.spoof(amount, ...) and blink().",
    "client_variations": "Rise 6.1.30 and 6.2.4 are the only reference clients with non-vanilla S20 code, both through the component plus NetworkAPI registration. Classic clients (Memeware, Nekoware, Sigma, Spicy, Tenacity) ship the stock handler.",
    "related": [
      "S14PacketEntity",
      "S18PacketEntityTeleport",
      "S1CPacketEntityMetadata"
    ]
  }
}