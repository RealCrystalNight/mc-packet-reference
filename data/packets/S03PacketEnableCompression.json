{
  "id": "S03PacketEnableCompression",
  "hex": "0x03",
  "dec": 3,
  "dir": "CLIENTBOUND",
  "state": "LOGIN",
  "name": "Enable Compression",
  "desc": "Enables zlib compression for all subsequent packets. Sets the compression threshold — packets >= this size are compressed.",
  "tags": [
    "login",
    "compression"
  ],
  "fields": [
    {
      "name": "compressionThreshold",
      "type": "VarInt",
      "desc": "Minimum packet size in bytes before compression is applied. -1 disables compression."
    }
  ],
  "encoding": [
    [
      "Threshold",
      "VarInt",
      "Typically 256 in vanilla; -1 = disabled"
    ]
  ],
  "mcp": [
    "NetHandlerLoginClient.handleEnableCompression()"
  ],
  "handler": "INetHandlerLoginClient",
  "notes": "After this packet, all future packets start with a VarInt prefix indicating the uncompressed length. If length=0, the data is uncompressed (smaller than threshold). If length>0, the remaining data is zlib-compressed and must be inflated.",
  "implementation": {
    "modules": [
      {
        "name": "Disabler",
        "found_in": [],
        "purpose": "Cancel or delay compression to prevent server-side anti-cheat from reading packets.",
        "how_it_works": "",
        "vanilla_hook": "EventReceivePacket"
      }
    ]
  }
}