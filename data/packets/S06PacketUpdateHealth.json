{
  "id": "S06PacketUpdateHealth",
  "hex": "0x06",
  "dec": 6,
  "dir": "CLIENTBOUND",
  "state": "PLAY",
  "name": "Update Health",
  "desc": "Updates the player's health, food level, and saturation. Sent ~20 times per second during combat.",
  "tags": [
    "player",
    "health",
    "food",
    "combat"
  ],
  "fields": [
    {
      "name": "health",
      "type": "float",
      "desc": "Current health (0.0-20.0). ≤ 0.0 = death screen."
    },
    {
      "name": "foodLevel",
      "type": "VarInt",
      "desc": "Current food level (0-20)"
    },
    {
      "name": "saturationLevel",
      "type": "float",
      "desc": "Food saturation (0.0-5.0)"
    }
  ],
  "encoding": [
    [
      "Health",
      "float",
      "0.0-20.0"
    ],
    [
      "Food",
      "VarInt",
      "0-20"
    ],
    [
      "Saturation",
      "float",
      "0.0-5.0"
    ]
  ],
  "mcp": [
    "NetHandlerPlayClient.handleUpdateHealth()"
  ],
  "handler": "INetHandlerPlayClient",
  "notes": "When health ≤ 0, the client shows the death screen overlay and sends C16PacketClientStatus(PERFORM_RESPAWN) on respawn button click.",
  "implementation": {
    "modules": [
      {
        "name": "AutoRespawn",
        "found_in": [
          "Tenacity 5.1",
          "Sigma 3.9",
          "Gugustus"
        ],
        "purpose": "Detect health changes. AutoRespawn triggers on health<=0. AutoPot uses health thresholds. GodMode cancels health update.",
        "how_it_works": "",
        "detailed_code": "if (event.getPacket() instanceof S06PacketUpdateHealth) {\n  S06PacketUpdateHealth s06 = (S06PacketUpdateHealth) event.getPacket();\n  if (s06.getHealth() <= 0) autoRespawn();\n  if (s06.getHealth() < threshold) autoPot();\n}",
        "vanilla_hook": "EventReceivePacket"
      },
      {
        "name": "AutoPot",
        "found_in": [
          "Tenacity 5.1",
          "Sigma 3.9",
          "Gugustus"
        ],
        "purpose": "Detect health changes. AutoRespawn triggers on health<=0. AutoPot uses health thresholds. GodMode cancels health update.",
        "how_it_works": "",
        "detailed_code": "if (event.getPacket() instanceof S06PacketUpdateHealth) {\n  S06PacketUpdateHealth s06 = (S06PacketUpdateHealth) event.getPacket();\n  if (s06.getHealth() <= 0) autoRespawn();\n  if (s06.getHealth() < threshold) autoPot();\n}",
        "vanilla_hook": "EventReceivePacket"
      },
      {
        "name": "Regen",
        "found_in": [
          "Tenacity 5.1",
          "Sigma 3.9",
          "Gugustus"
        ],
        "purpose": "Detect health changes. AutoRespawn triggers on health<=0. AutoPot uses health thresholds. GodMode cancels health update.",
        "how_it_works": "",
        "detailed_code": "if (event.getPacket() instanceof S06PacketUpdateHealth) {\n  S06PacketUpdateHealth s06 = (S06PacketUpdateHealth) event.getPacket();\n  if (s06.getHealth() <= 0) autoRespawn();\n  if (s06.getHealth() < threshold) autoPot();\n}",
        "vanilla_hook": "EventReceivePacket"
      },
      {
        "name": "GodMode",
        "found_in": [
          "Tenacity 5.1",
          "Sigma 3.9",
          "Gugustus"
        ],
        "purpose": "Detect health changes. AutoRespawn triggers on health<=0. AutoPot uses health thresholds. GodMode cancels health update.",
        "how_it_works": "",
        "detailed_code": "if (event.getPacket() instanceof S06PacketUpdateHealth) {\n  S06PacketUpdateHealth s06 = (S06PacketUpdateHealth) event.getPacket();\n  if (s06.getHealth() <= 0) autoRespawn();\n  if (s06.getHealth() < threshold) autoPot();\n}",
        "vanilla_hook": "EventReceivePacket"
      }
    ]
  }
}