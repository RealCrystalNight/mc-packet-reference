{
  "module": "AntiBot",
  "client": "Rise 5.99",
  "category": "antibot",
  "source_rel": "dev/rise/module/impl/combat/AntiBot.java",
  "overview": "Rise 5.99's AntiBot is a configurable bot filter with nine independent boolean checks under a Custom mode and a fixed Hypixel preset, all evaluated from onPreMotion every tick. Each check writes the entity's bot flag, and every flagged player is collected into the static bots list that the Aura module consults when selecting targets. The Custom checks cover visibility ticks, movement, duplicate and invalid names, entity ids and tab-list ping; the Hypixel preset looks for invisible entities that teleported upward or hover above the player. onEnable conservatively marks every player as a bot, and onDisable clears all flags and the list.",
  "sections": [
    {
      "h": "Architecture",
      "p": "The module declares a ModeSetting with Custom and Hypixel values and nine BooleanSettings: noMove, duplicateName, ticksExisted, ticksVisible, invalidName, duplicateID, negativeID, distance and ping. onUpdateAlwaysInGui hides all nine unless Custom is selected, so the GUI only exposes what the current mode uses. onPreMotion clears the static bots list, then iterates mc.theWorld.playerEntities, skips the local player, applies the active mode's checks, and appends any player whose bot flag is set. The public static List<Entity> bots is the module's only output, and Aura reads it inside its target filter. onEnable marks every player in the world as a bot so nothing is attacked until the checks clear it, and onDisable resets every entity flag."
    },
    {
      "h": "Bot detection heuristics",
      "p": "The Custom checks each set player.bot directly. ticksVisible flags anyone with ticksExisted at or below 160 or ticksVisible at or below 160, resetting the visible counter to 0 while the entity is invisible to the player or fully invisible. noMove flags players younger than 10 ticks and unflags them as soon as lastTickPosX or lastTickPosZ differs from the current position, so stationary spawns stay flagged. duplicateName flags the second player with a repeated name, invalidName flags any name containing a character outside A-Za-z0-9 and underscore, ticksExisted flags non-positive ages, duplicateID and negativeID flag repeated or negative entity ids, and distance unflags players more than 200 blocks away while flagging anyone younger than 5 ticks. ping flags players whose tab-list NetworkPlayerInfo reports a negative response time.\n\nThe Hypixel preset runs two rules, both requiring the entity to be invisible and on the ground: a rise in posY minus lastTickPosY greater than 1.5 marks a bot, as does a position between 4 and 10 blocks above the local player with a per-tick rise under 0.5. Both target the server's practice of spawning anti-cheat bots in the air above players and teleporting them into position. The checks are deliberately loose, and the module compensates by defaulting to the bot=true state for every player on enable, so a clean filter needs the movement or visibility checks to clear real players over the first few seconds."
    },
    {
      "h": "Interaction with killaura targeting",
      "p": "Aura's target filter rejects any entity with ticksExisted below 2 and any entity contained in AntiBot.bots, so a flagged player is never selected by getTargets and never attacked by the Single, Switch or Multi paths. Because onPreMotion rebuilds the list every tick and onEnable starts from everyone-is-a-bot, the filter is a one-tick-deep snapshot: an entity is excluded only while a check keeps its flag set in the same tick the aura selects. The multi-target path benefits the most, since a bot that slips into the list would otherwise receive a C02 in the same tick as a real player, and the Multi-mode angle signature would include the bogus target."
    }
  ],
  "packets": [],
  "files": [
    {
      "label": "AntiBot.java (mode selector and nine checks)",
      "rel": "dev/rise/module/impl/combat/AntiBot.java",
      "explanation": "The whole module is this one class. The ModeSetting chooses Custom or Hypixel, onUpdateAlwaysInGui toggles the visibility of the nine BooleanSettings, and onPreMotion clears bots and re-runs the selected mode's checks over mc.theWorld.playerEntities, excluding the local player. Every check writes the entity's bot field, and any player with bot set is appended to the static bots list that the combat modules read. The module imports no utility classes: name scanning, id comparison and the ping lookup through mc.getNetHandler().getPlayerInfo are all inlined.\n\nThe toggle lifecycle is part of the filter's behavior. onEnable sets bot = true on every player in the world, so the filter starts maximally conservative and relies on the per-tick checks to clear real players; onDisable clears bots and resets every entity flag, so a toggle-off restores normal targeting immediately. The Hypixel mode is the only branch that reads positions: it needs invisible, grounded entities that either jumped more than 1.5 blocks or hover 4 to 10 blocks above the local player with minimal vertical movement."
    }
  ],
  "countering": [
    {
      "label": "Hawk EntityInteractReach (interaction distance gate)",
      "ac": "HawkAnticheat-Hawk",
      "rel": "src/me/islandscout/hawk/check/interaction/entity/EntityInteractReach.java",
      "explanation": "EntityInteractReach is Hawk's distance gate for every entity interaction, including attacks. It measures from the attacker's eye position to the victim's hitbox and compares against MAX_REACH, 3.1 blocks in survival and 5.0 in creative. The check runs on the interaction packet itself rather than the follow-up move, so the distance uses the server's current tracked positions, and the victim box comes from WrappedEntity.getHitbox at the interaction location.\n\nRise's Aura attacks at its rotation range, and its Extended Range mode streams up to 100 C04 position packets that step the server-side position toward the target before attacking, then streams them back afterward. The position-walk works only if the server actually processes each C04 as movement, and Hawk measures from the position it has accepted for the player, so the walk must close the entire gap or the attack still exceeds 3.1. The client-side bot filter changes nothing for this check: it validates the geometry of each use-entity packet that is sent, and a bot that is filtered out simply never produces a packet."
    },
    {
      "label": "NCP Fight Angle (rotation versus movement)",
      "ac": "Updated-NoCheatPlus-NoCheatPlus",
      "rel": "NCPCore/src/main/java/fr/neatmonster/nocheatplus/checks/fight/Angle.java",
      "explanation": "NCP's Angle check records every attack as an AttackLocation holding the player location, the damaged entity's unique id and the timestamp, and compares consecutive entries. Each new attack adds the squared movement between locations, the absolute yaw difference between the attack frames, and the elapsed time; a target switch counts when the unique id differs and the yaw difference exceeds 30 degrees. Entries older than maxTimeDiff are dropped, and the averages are scaled by server lag.\n\nThe violation logic then compares the average yaw change against the average movement and time: a player who stays nearly still while snapping the view between targets accumulates yaw deltas with almost no movement, which is the rotation signature of an aura. The check needs at least two attacks in the window to fire, and target switching with yaw changes over 30 degrees is the strongest signal.\n\nRise's Multi mode emits several C02 attacks at different entities within the same tick, and even Single mode snaps serverYaw and serverPitch into PreMotion so the server observes full-speed rotation changes while the position barely moves. With default snap rotations the per-attack yaw delta between distinct targets is large and the movement between attacks is near zero, exactly the averageYaw-versus-averageMove imbalance the check scores. The bot filter trims the candidate list, but Multi mode still cycles real targets at full rotation speed, so the signature survives filtering."
    },
    {
      "label": "Arc KillAura (multi-target angle and speed)",
      "ac": "Vrekt-Arc-v2",
      "rel": "src/main/java/me/vrekt/arc/check/combat/KillAura.java",
      "explanation": "Arc's KillAura check combines a Direction angle test, a Multi target-switch angle test, a NoSwing timing test, a Reach distance test and a Speed attack-rate test. Direction compares the look vector with the vector to the attacked entity; Multi compares the angle to the previous target when the target changed within reach; NoSwing flags an attack more than max-swing-time after the last arm swing; Reach subtracts the target's velocity from the distance; Speed counts attacks and attack packets per second.\n\nThe Multi sub-check is the direct counter to Rise's Multi mode: consecutive attacks at different entities inside max-reach produce an angle difference between the two target vectors that exceeds max-direction-angle unless the targets are nearly collinear, and the check fires 'Kill Aura (Multi)'. The Speed sub-check counters the CPS-based timer: sustained clicking above max-attacks per second accumulates violations, and checkFrequency separately scores the raw attack-packet count, which the block modes inflate with C08 and C07 traffic around each C02.\n\nThe silent-rotation path does not help here: the server sees the spoofed yaw and pitch, and Direction measures the angle between that spoofed look and the target. Rise's random rotation settings can push the per-attack angle under the limit, but Multi-mode target switches remain the exposed signal. The bot filter never interacts with these counters, since they score only the packets that are actually sent."
    },
    {
      "label": "Grim Reach (reconstructed hitbox intercept)",
      "ac": "GrimAnticheat-Grim",
      "rel": "common/src/main/java/ac/grim/grimac/checks/impl/combat/Reach.java",
      "explanation": "Grim's Reach validates each attack against the entity box as reconstructed from spawn and move packets, with lag compensation. The check enumerates the possible look directions from the current and previous yaw and pitch, extends each ray to the reach limit plus 3 blocks, and requires an intersection with the box through ReachUtils.calculateIntercept. The minimum reach to the box is computed by ReachUtils.getMinReachToBox, and the threshold for a violation is 0.0005, with block-impossible-hits cancelling attacks no look direction could have landed.\n\nRise's Extended Range mode depends on moving the server-side position with C04 streams, which changes the eye position Grim uses, so the walk genuinely shrinks the measured distance while it is in effect. The counter is the reconstruction itself: the entity box is interpolated between replicated positions, the look is taken from the movement packets, and an attack must intersect the box within the limit, so a through-wall hit or a hit from beyond the box fails. The threshold makes borderline hits measurable rather than binary.\n\nThe bot filter reduces which entities produce C02 packets, but every packet that survives is checked against the replicated box, and the aura's bypass settings do not alter the geometry Grim sees. Filtered-out bots simply produce no packets, which is the same as not attacking them at all."
    }
  ]
}
