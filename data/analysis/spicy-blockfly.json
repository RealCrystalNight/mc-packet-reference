{
  "module": "Block Fly",
  "client": "Spicy",
  "category": "blockfly",
  "source_rel": "info/spicyclient/modules/movement/BlockFly.java",
  "overview": "Spicy's Block Fly is a safe-walk block placer. It hijacks the EventSneaking hook inside Entity.moveEntity to force the sneak edge-clamp while the player is grounded, cancels incoming S2F set-slot packets so server inventory corrections never apply, rewrites every outbound C03 look to the last placement rotation, and, whenever the cell directly below the player is air with solid ground two cells down, picks a block from the hotbar, switches to it locally and places it at the feet through the vanilla player controller. The placement look is computed by an in-module aim routine that pins pitch toward 90 degrees, and the look latch makes the server see the player looking at each placement.",
  "sections": [
    {
      "h": "The sneak clamp",
      "p": "EventSneaking fires from Entity.moveEntity with the vanilla gate flag = onGround && isSneaking() && instanceof EntityPlayer, and the fork lets listeners override both the sneaking state and the offset used by the edge-clamp loop. The module forces sneaking true with offset -1 whenever the moving entity is grounded and an EntityPlayer, so the clamp loop tests the bounding box shifted one block down and the player cannot walk off an edge; the clamp is released in air, where the module sets sneaking false, and the whole handler skips while the client's killaura holds a target."
    },
    {
      "h": "Under-foot placement",
      "p": "The EventMotion post handler checks the block state one cell below the player. When that cell is air and the cell two below is solid, it scans hotbar slots 36-44 for an ItemBlock whose localized name does not contain chest or table, switches to it through inventory.setCurrentItem, and calls mc.playerController.onPlayerRightClick at the under-below position with facing UP and a hit vector from RotationUtils.getVectorForRotation, which produces a real C08 placement and a swing. Side placements probe a position offset along motionZ and walk the facing search through findFacingAndBlockPosForBlock, which scans the block neighbors and their neighbors for a solid anchor. The event yaw and pitch are then set from getRotationsHypixel, an atan2 aim at the block center with pitch boosted by 30 degrees and clamped to 90, and the same values latch into lastYaw and lastPitch."
    },
    {
      "h": "Wire shape",
      "p": "The EventSendPacket pre handler rewrites the yaw and pitch of every outbound C03PacketPlayer to lastYaw and lastPitch, so the server-side look stream reports the placement look while the module is placing. The S2F cancel drops every incoming set-slot packet, keeping the locally switched hotbar slot free of server corrections. The placement path emits at most one C08 per tick because every branch returns after its placement, and the C09 held-item change rides the vanilla syncCurrentPlayItem call when the tracked slot diverges. Motion itself is ordinary: the edge clamp is a client-side collision change and the C03 stream carries normal walk speeds."
    }
  ],
  "packets": [
    "C03PacketPlayer"
  ],
  "files": [
    {
      "label": "BlockFly.java (module)",
      "rel": "info/spicyclient/modules/movement/BlockFly.java",
      "explanation": "The module is four handlers. EventSneaking pre forces the grounded sneak clamp with offset -1, EventPacket pre cancels S2F set-slot packets, EventSendPacket pre rewrites every C03 yaw and pitch to the latched placement look, and EventMotion post runs the placement machine: the air-below check, the hotbar ItemBlock scan with the chest and table name filter, the local slot switch, the playerController.onPlayerRightClick call that emits the real C08, and the getRotationsHypixel aim latch.\n\nThe placement logic is strictly gated: the EventUpdate pre branch only runs the motion probes, and the actual placement fires only when the below cell is air and the two-below cell is solid (or a side anchor is found), so the module idles on flat ground and only places while crossing a gap or edge. Every branch returns after the first placement, capping the rate at one C08 per tick."
    },
    {
      "label": "MovementUtils.java (ground probes)",
      "rel": "info/spicyclient/util/MovementUtils.java",
      "explanation": "The module's placement cadence is gated by this class's isOnGround(height) probe, which tests the world collision boxes against the entity bounding box offset downward by the given height. The EventUpdate pre branch only proceeds when isOnGround(0.4) succeeds, and the inner motion test layers isOnGround(0.3) and isOnGround(0.25) probes plus a jump-key check, so the module waits for grounded stability before arming the placement logic.\n\nThe helper also carries the fork's motion conventions used across the movement modules, including setMotion with 45-degree strafe folding and the input-derived direction model, which places this module's placement gating in the same util family as the fly, longjump and sprint modules of the same client."
    }
  ],
  "countering": [
    {
      "label": "FastPlace (placement rate)",
      "ac": "Updated-NoCheatPlus-NoCheatPlus",
      "rel": "NCPCore/src/main/java/fr/neatmonster/nocheatplus/checks/blockplace/FastPlace.java",
      "explanation": "NoCheatPlus's blockplace FastPlace is invoked from the Bukkit BlockPlaceEvent path, and the module's placements are genuine: playerController.onPlayerRightClick sends a real C08 with a block position and facing, the server places the block, and the event fires. The check scores into BlockPlaceData.fastPlaceBuckets, an ActionFrequency(2, 1000) two-second window, plus a config-driven short-term tick counter, with the violation being the larger of the full-window excess and the short-term excess.\n\nWhile the module crosses gaps it can place once per tick, up to 20 placements per second, which is roughly five times the vanilla 4-per-second rate, so a sustained crossing scores around five times the vanilla baseline and trips both the window and the short-term path. The honest boundary is the gate: on flat ground with solid blocks below, the module idles and the stream stops, so the check only scores the crossing phases."
    },
    {
      "label": "Direction (placement look validation)",
      "ac": "Updated-NoCheatPlus-NoCheatPlus",
      "rel": "NCPCore/src/main/java/fr/neatmonster/nocheatplus/checks/blockplace/Direction.java",
      "explanation": "NoCheatPlus's blockplace Direction extends AbstractBlockDirectionCheck and validates the placement look against the interacted block face, requiring the player's look ray to intersect the face they claim and accumulating penalties for mismatches with a 0.9 decay. The module computes the placement look with getRotationsHypixel, an atan2 aim at the block center with the pitch boost clamped to 90, and latches the same values into every C03, so the server-side look ray is aimed at the placed face by construction.\n\nThis entry is the honest boundary: the module deliberately aligns look and placement, so Direction passes it, and a scaffold that aimed elsewhere would be exactly what the check punishes. The pitch clamp at 90 keeps the ray vertical, which still intersects the UP face of the under-below block from the player's eye height, so the validation has nothing to reject."
    },
    {
      "label": "Reach (placement distance)",
      "ac": "Updated-NoCheatPlus-NoCheatPlus",
      "rel": "NCPCore/src/main/java/fr/neatmonster/nocheatplus/checks/blockplace/Reach.java",
      "explanation": "NoCheatPlus's blockplace Reach measures the eye-to-block distance against the survival distance default of 5.2 blocks (5.6 creative) in this tree, cancelling placements beyond the limit and reducing repeat hits inside the reachMod band. The module places at the cell two below the feet, so the eye at roughly posY plus 1.62 to a block center at posY minus 1.5 sits around 3.6 blocks away, comfortably inside the survival bound.\n\nThe entry is a boundary in the same family as Direction: the module never extends reach because it only ever targets the cell under the player, and the under-foot geometry keeps every placement legal. A variant that reached several cells out for tower building would be the shape this check exists to cancel, and the module's own geometry avoids it by design."
    },
    {
      "label": "BlockInteractSpeed (same-tick interaction)",
      "ac": "HawkAnticheat-Hawk",
      "rel": "src/me/islandscout/hawk/check/interaction/terrain/BlockInteractSpeed.java",
      "explanation": "Hawk's BlockInteractSpeed punishes with vl 1 and cancel whenever a world interaction arrives on the same server tick as the previous one, keeping a per-player lastPlaceTick map and rewarding every interaction that lands on a later tick. The module caps at one C08 per tick because every placement branch returns immediately, so each interaction lands on a fresh tick and the check rewards the stream instead of punishing it.\n\nThis is the same-tick boundary that separates a per-tick placement module from a burst placement module: a stream of one placement per tick is exactly what BlockInteractSpeed does not flag, while two interactions in one tick (the shape a double-placement or a placement plus a use produces) trips it instantly. The honest statement is that this check is structurally blind to the module's cadence, and the real pressure comes from the FastPlace window ledger."
    }
  ]
}
