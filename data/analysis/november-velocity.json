{
  "module": "Velocity",
  "client": "November Recode 2.0",
  "category": "velocity",
  "source_rel": "feature/module/impl/combat/VelocityModule.java",
  "overview": "November Recode 2.0's Velocity module is a single class with eight modes. REDUCE and REVERSE scale the S12 vector in place with separate horizontal and vertical percentages; CANCEL drops the packet entirely; GRIM cancels the S12 and then eats the S32 confirmations and their C0F echoes so the transaction clock never confirms the knockback arrived; VULCAN cancels the S12 and the one C0F that follows; WATCHDOG_SAFE cancels the S12 but re-applies the vertical component to the player's motion while on the ground; and INTAVE and WATCHDOG are stubs that do nothing in this build. The GRIM mode is the most elaborate: it keeps a queue of cancelled transaction action numbers and matches outgoing C0F uids against it, cancelling the echoes of exactly the transactions it swallowed.",
  "sections": [
    {
      "h": "Architecture",
      "p": "The module registers three listeners. EventUpdate runs every tick and clears the funny flag once the transaction queue drains; EventPacket.In handles S12 and S32; EventPacket.Out handles C0F. The settings are a Mode enum plus Horizontal and Vertical floats (0.01 to 200.0, default 50.0) whose visibility lambda only shows them in REDUCE and REVERSE. State is the ConcurrentLinkedQueue of Short action numbers and the funny boolean; disable() clears both, and the null checks at the top of the receive listener reset the state whenever the world or player changes.\n\nThe enum drives the dispatch with switch expressions over the packet branches. The module is registered with the name Velocity, the description 'Reduces or cancels out taken velocity', and category Category.COMBAT through the @Register declaration. The transaction queue is a ConcurrentLinkedQueue because the receive listener and the send listener can run on different threads."
    },
    {
      "h": "Key techniques",
      "p": "REDUCE and REVERSE scale in place: each motion component is multiplied by the percentage over 100.0, truncated back to int, and written with setMotionX/Y/Z, and REVERSE then negates all three. Because the packet fields are ints on the 8000 fixed-point scale, the cast truncates the fractional part of each scaled value. CANCEL, GRIM and VULCAN call setCanceled(true) on the receive event so the vanilla handler never applies the vector. WATCHDOG_SAFE cancels the S12 but, when the player is on the ground, adds motionY over 8000.0 to the player's live motionY, keeping the vertical launch while discarding the horizontal push.\n\nGRIM sets funny on the cancelled S12; every S32 that arrives while funny is cancelled and its action number queued, and every outgoing C0F whose uid matches a queued number is cancelled, so the server's transaction accounting for that window is silently dropped. VULCAN is the simpler variant: one cancelled S12 arms funny and the very next C0F is cancelled. INTAVE and WATCHDOG carry TODO comments and no code, and the horizontal and vertical settings do not affect CANCEL, GRIM, VULCAN or WATCHDOG_SAFE."
    },
    {
      "h": "Transaction-level bypass",
      "p": "The GRIM mode is aimed at anticheats that bracket velocity with transactions, notably Grim's KnockbackHandler, which sandwiches the S12 between two server transactions and treats the echo of the second as proof the knockback arrived. By cancelling the S32 receipts and the matching C0F echoes, the module prevents that promotion: the velocity stays in the uncertain, not-yet-arrived bucket and is never required by the prediction engine, so the missing knockback never produces an offset. The funny flag is cleared only when the queue drains, so the eating continues until every swallowed transaction has been accounted for.\n\nThe cost is a desynced transaction stream: every cancelled receipt and echo moves the client's transaction ledger away from the server's, which transaction-order checks and inventory and UI confirmations notice over time. The queue is deliberately cleared on disable and on world null so the desync cannot persist across sessions, and the update listener clears funny once the queue is empty. VULCAN leaves the transaction stream mostly intact, which is why it is labeled as working on older Vulcan versions only."
    }
  ],
  "packets": [
    "S12PacketEntityVelocity",
    "S32PacketConfirmTransaction",
    "C0FPacketConfirmTransaction",
    "C03PacketPlayer"
  ],
  "files": [
    {
      "label": "VelocityModule.java (module)",
      "rel": "feature/module/impl/combat/VelocityModule.java",
      "explanation": "The whole module is this one file. The receive listener branches on the packet type: S12 carries the mode dispatch (REDUCE and REVERSE scale the vector, CANCEL and GRIM and VULCAN cancel it, WATCHDOG_SAFE applies the vertical component on the ground), and S32 carries the GRIM transaction eating, which cancels the receipt and queues the action number when funny is set. The send listener matches outgoing C0F uids against the queue and cancels the echoes, and the update listener clears funny when the queue drains.\n\nThe REDUCE and REVERSE scaling is worth noting for its precision: the packet fields are ints on the 8000 fixed-point scale, so multiplying by a percentage and casting back to int truncates the fractional part of every component, which means the scaled vector can differ from the exact percentage by up to one fixed-point unit per axis. REVERSE negates all three axes after the percentage scaling, producing a full redirection of the knockback.\n\nThe mode enum, the settings, the queue and the funny flag are the entire state surface. The visibility lambda on the horizontal and vertical settings hides them in every mode except REDUCE and REVERSE, so the GUI only exposes the sliders where they matter. The INTAVE and WATCHDOG modes are honest stubs with TODO comments ('i have no clue' and 'frappy or plus add this i dont have alts'), and the module's disable() hook clears the queue and flag so the transaction eating cannot leak into the next session."
    }
  ],
  "countering": [
    {
      "label": "KnockbackHandler (AntiKB, prediction offset)",
      "ac": "GrimAnticheat-Grim",
      "rel": "common/src/main/java/ac/grim/grimac/checks/impl/velocity/KnockbackHandler.java",
      "explanation": "Grim's KnockbackHandler is the exact mechanism November's GRIM mode was written against. The check sandwiches every ENTITY_VELOCITY between two server transactions: sendTransaction() before the packet and a second transaction queued through tasksAfterSend. The velocity is recorded in firstBreadMap keyed by the first transaction id, and tickKnockback promotes it into lastKnockbackKnownTaken only when a transaction with a larger id arrives, which is how Grim proves the packet reached the client. From that promotion onward the vector is required movement, and onPredictionComplete flags when the prediction offset exceeds offsetToFlag (default 0.001), with a setback once the offset reaches 0.1 or the accumulated threshold (decay 0.999, ceiling 4) hits maxAdvantage 1.\n\nNovember's GRIM mode attacks the promotion itself. It cancels the S32 receipts while funny and queues their action numbers, then cancels the matching C0F echoes on the send side, so the second transaction of the sandwich never confirms and the velocity stays in the uncertain firstBreadMap bucket, never required and never flagged. The same pattern applies to the first bread: calculateFirstBreadKnockback only returns a velocity whose transaction has been confirmed, and an eaten echo leaves it unknown. This is the check's genuine blind spot, and the mode's entire design exists to exploit it.\n\nThe cost is the transaction ledger. Every cancelled receipt and echo moves the client's accounting away from the server's, and Grim's transaction tracking validates its own negative ids, so a confirmation that matches nothing or a missing echo is a stream anomaly that other checks notice. The queue is cleared on disable and on world null, which bounds the desync to the active session, and the update listener clears funny once the queue drains. A player who eats transactions forever will eventually trip the transaction-order surface even if the velocity offset never accrues."
    },
    {
      "label": "AntiVelocityBasic (pending knockback matching)",
      "ac": "HawkAnticheat-Hawk",
      "rel": "src/me/islandscout/hawk/check/movement/position/AntiVelocityBasic.java",
      "explanation": "Hawk's MoveEvent matches every move's current velocity against the pending knockback list. Each entry is a vector plus the server send time and stays eligible while the elapsed time is within the player's ping plus 200ms. A match requires the move's Y to be within 0.01 of the knockback's Y (unless the player collides, jumps, swims or steps) and X and Z to fall inside maxDiscrepancy, computed from walk speed, speed potions and the sprint multiplier plus an 0.003 epsilon. When an entry ages past ping plus 200ms without a match, failedKnockback is set outside vehicles, and AntiVelocityBasic.check punishes at 5 VL with the 'may be using anti-velocity (basic)' flag.\n\nNovember's CANCEL, GRIM and VULCAN modes all leave the pending entry unmatched, so it expires and flags. REDUCE and REVERSE scale the vector client-side, but the check compares against the original entry from the server's own pending list, so a 50% reduction produces a move that fails the X and Z match whenever the gap exceeds maxDiscrepancy, and the stronger the original hit the larger the absolute gap. WATCHDOG_SAFE is the partial exception: it applies the full vertical component on the ground, so the Y match can succeed and only the horizontal plane diverges.\n\nThe class comment claims detection down to a 10% difference, which is the size of the tolerance window relative to a typical knockback. Small REDUCE percentages that keep the movement inside maxDiscrepancy pass, and the ping-plus-200ms window forgives knockback the player simply has not moved into yet. The check is content-blind and needs no prediction, only the pending list and the move deltas, which is why every cancel mode is caught."
    },
    {
      "label": "VelocityA (sent vector vs reported rise)",
      "ac": "AimbotPvP-azure-anticheat",
      "rel": "src/main/java/us/skidrevenant/azure/check/checks/combat/velocity/VelocityA.java",
      "explanation": "Azure's VelocityA compares the vertical component of the S12 the server sent (field c divided by 8000.0, with damage-cause exclusions for starvation, fire tick, drowning, poison, wither, thorns, fall, lava and fire) against the rise the player reports. onMove only proceeds when the player rose, and flags when deltaY is positive, below the 0.42 jump height and below 77% of the recorded vertical velocity, printing the percentage and applying a 10000ms cooldown with 4 VL. The denominator comes from the server's own packet, so client-side scaling cannot change it.\n\nREDUCE at the default 50% produces a rise around half the sent vector, ratio 0.5, which flags as Velocity Type A (50%); any vertical percentage from 1 through 76 flags, while 80% and above passes. REVERSE presents a negative rise, so the player appears to drop and the check's rise gate skips it entirely. WATCHDOG_SAFE keeps the full vertical component on the ground, so its rise matches the sent vector and passes, which is the mode's design intent: keep the vertical launch, drop only the horizontal push.\n\nCANCEL, GRIM and VULCAN never rise, so the from.getY() >= to.getY() early return fires and nothing is flagged. The 0.42 cap is the safeguard against jumping, and the exclusions prevent false flags on fall, fire and drowning damage. Among November's modes, REDUCE at partial vertical is the only one this check reliably catches; the cancel family is its documented blind spot."
    },
    {
      "label": "VelocityListener (claimed velocity feed)",
      "ac": "CheatSolutions-Spartan-AntiCheat",
      "rel": "src/main/java/ai/idealistic/spartan/listeners/protocol/VelocityListener.java",
      "explanation": "Spartan's VelocityListener claims every ENTITY_VELOCITY for the self entity at MONITOR priority: it reads the three fixed-point components divided by 8000.0 through BackPlib.getSafeInt, builds a CPlayerVelocityEvent, forwards the packet's cancellation state, and dispatches it on the CheckThread through VelocityEvent.event. VelocityEvent stores the claim in claimedVelocity, pushes it onto the claimedVeloGravity and claimedVeloSpeed queues (each capped at two entries) and calls executeRunners, which feeds Spartan's movement checks.\n\nThe movement checks compare the reported movement against the claimed vector, with gravity applied on the gravity queue, so a client that cancels the S12 receive-side is still measured against the vector the server sent. The listener records the original packet contents, so REDUCE's client-side scaling does not change what Spartan expects; a 50% vertical reduction shows up as a movement deficit against the full claim. CANCEL, GRIM and VULCAN produce the same deficit from a complete absence of movement.\n\nWATCHDOG_SAFE is the closest to a pass: it preserves the vertical component on the ground, so the gravity-profile match succeeds for the rise and only the horizontal plane diverges, which lands under the checks' tolerances for weak hits. The queues hold only the last two claims, so a long gap between hits gives the checks no baseline, and the bukkit-side twin listener keeps the two feeds consistent through the packetsEnabled flag in PlayerProtocol. The listener itself is a feed, not a verdict; the flagging lives in the movement checks that consume the claim."
    }
  ]
}
