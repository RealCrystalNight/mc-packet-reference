{
 "overview": "Server-side per-player survival, creative, adventure and spectator interaction controller. Validates dig timing, block harvesting, item use and block activation from NetHandlerPlayServer. Sends corrections and progress animations plus gamemode broadcasts to other players.",
 "methods": [
  {
   "detail": "Applies capability preset, pushes abilities to owner, then broadcasts S38PacketPlayerListItem UPDATE_GAME_MODE to every client.",
   "name": "setGameType",
   "packets": [
    "S38"
   ],
   "purpose": "Change gamemode and broadcast to all players"
  },
  {
   "detail": "Creative calls tryHarvestBlock directly; survival starts isDestroyingBlock and calls world.sendBlockBreakProgress for S25 animation.",
   "name": "onBlockClicked",
   "packets": [
    "S25"
   ],
   "purpose": "Begin survival dig or instant creative break"
  },
  {
   "detail": "Advances curblockDamage, sends staged 0-10 break progress or -1 reset through world, harvests when hardness threshold reached.",
   "name": "updateBlockRemoving",
   "packets": [
    "S25"
   ],
   "purpose": "Tick ongoing dig progress and finish harvest"
  },
  {
   "detail": "If hardness above 0.7 finishes immediately with reset animation, else parks state in receivedFinishDiggingPacket for tick completion.",
   "name": "blockRemoving",
   "packets": [
    "S25"
   ],
   "purpose": "Handle stop-digging packet mid-break"
  },
  {
   "detail": "Clears isDestroyingBlock and sends block-break progress -1 for the tracked position to watching clients.",
   "name": "cancelDestroyingBlock",
   "packets": [
    "S25"
   ],
   "purpose": "Abort dig and clear crack overlay"
  },
  {
   "detail": "Enforces adventure canDestroy rules, plays AuxSFX, removes block; in creative sends S23PacketBlockChange so client matches server.",
   "name": "tryHarvestBlock",
   "packets": [
    "S23"
   ],
   "purpose": "Remove block, drop loot and correct creative client"
  },
  {
   "detail": "Calls useItemRightClick, restores stack size and damage in creative, and resyncs inventory container when not using item.",
   "name": "tryUseItem",
   "packets": [],
   "purpose": "Apply right-click item use with creative restore"
  },
  {
   "detail": "Spectator only opens chests; others try onBlockActivated first unless sneaking, then delegates to stack.onItemUse.",
   "name": "activateBlockOrUseItem",
   "packets": [],
   "purpose": "Activate block or place held item"
  }
 ],
 "packet_usage": [
  {
   "direction": "handled",
   "id": "C07/C08",
   "trigger": "NetHandlerPlayServer dig and block-place drive these methods"
  },
  {
   "direction": "sent",
   "id": "S25",
   "trigger": "sendBlockBreakProgress in dig start, tick and cancel"
  },
  {
   "direction": "sent",
   "id": "S23",
   "trigger": "tryHarvestBlock creative correction to breaker"
  },
  {
   "direction": "sent",
   "id": "S38",
   "trigger": "setGameType UPDATE_GAME_MODE broadcast"
  }
 ],
 "internal_fields": [
  {
   "name": "theWorld",
   "role": "Server world where block edits are applied"
  },
  {
   "name": "thisPlayerMP",
   "role": "Owning player for abilities and packet replies"
  },
  {
   "name": "gameType",
   "role": "Current survival, creative, adventure or spectator mode"
  },
  {
   "name": "isDestroyingBlock",
   "role": "Whether survival dig progress is active"
  },
  {
   "name": "receivedFinishDiggingPacket",
   "role": "Pending finish when stop arrived early"
  },
  {
   "name": "durabilityRemainingOnBlock",
   "role": "Last sent 0-10 crack stage to avoid resends"
  }
 ],
 "peers": [
  "EntityPlayerMP",
  "WorldServer",
  "WorldSettings",
  "NetHandlerPlayServer",
  "TileEntityChest"
 ],
 "methods_detailed": [
  {
   "name": "setGameType",
   "detail_long": "setGameType changes the owned player's gamemode and broadcasts it to the whole server. It stores the new WorldSettings.GameType in gameType for later survivalOrAdventure and isCreative checks. It calls type.configurePlayerCapabilities on thisPlayerMP.capabilities to set fly, edit, and damage flags. It calls thisPlayerMP.sendPlayerAbilities to push those flags to the owner, updating its own client permissions. It then broadcasts S38PacketPlayerListItem with Action UPDATE_GAME_MODE and an array containing thisPlayerMP via sendPacketToAllPlayers. Every connected client receives S38 and updates the tab-list gamemode icon plus adventure interaction rules for that player. The owner also gets the broadcast in addition to the direct abilities packet. No block or inventory change happens here.",
   "code": "    public void setGameType(WorldSettings.GameType type)\n    {\n        this.gameType = type;\n        type.configurePlayerCapabilities(this.thisPlayerMP.capabilities);\n        this.thisPlayerMP.sendPlayerAbilities();\n        this.thisPlayerMP.mcServer.getConfigurationManager().sendPacketToAllPlayers(new S38PacketPlayerListItem(S38PacketPlayerListItem.Action.UPDATE_GAME_MODE, new EntityPlayerMP[] {this.thisPlayerMP}));\n    }",
   "code_walkthrough": "Storing gameType updates local mode used by dig and use guards. configurePlayerCapabilities maps survival, creative, adventure, and spectator to capability booleans. sendPlayerAbilities pushes those flags to the owner client. Broadcasting S38 UPDATE_GAME_MODE informs all other clients of the new mode for rendering and permissions."
  },
  {
   "name": "onBlockClicked",
   "detail_long": "onBlockClicked starts block breaking when the client sends a dig-start packet. In creative it tries extinguishFire first to put out fire without breaking, and if not fire it calls tryHarvestBlock for instant removal. In survival and adventure it loads the target Block and enforces adventure canDestroy rules via isAllowEdit and held stack canDestroy, returning early for spectator or disallowed tools. It extinguishes fire at the position, snapshots initialDamage from curblockDamage, and queries getPlayerRelativeBlockHardness for dig speed. It notifies the block via onBlockClicked for doors and special handling. If hardness is at least 1.0 it calls tryHarvestBlock immediately for instant crops or soft blocks. Otherwise it sets isDestroyingBlock true, records field_180240_f position, and calls theWorld.sendBlockBreakProgress with staged crack 0 to 10. That call emits S25PacketBlockBreakAnim to watching clients so all see the crack overlay grow.",
   "code": "    public void onBlockClicked(BlockPos pos, EnumFacing side)\n    {\n        if (this.isCreative())\n        {\n            if (!this.theWorld.extinguishFire((EntityPlayer)null, pos, side))\n            {\n                this.tryHarvestBlock(pos);\n            }\n        }\n        else\n        {\n            Block block = this.theWorld.getBlockState(pos).getBlock();",
   "code_walkthrough": "The isCreative branch handles instant break versus fire extinguish without progress packets. The else branch loads the server block for survival hardness and adventure checks. Adventure validation returns early when the held item cannot destroy the block. Later code sets isDestroyingBlock and sends S25 crack progress to trackers."
  },
  {
   "name": "updateBlockRemoving",
   "detail_long": "updateBlockRemoving ticks every server tick to advance survival digging and emit S25 progress. It increments curblockDamage as the tick counter for both pending-finish and active-dig paths. If receivedFinishDiggingPacket is true meaning stop-dig arrived early, it computes elapsed ticks, loads the Block at field_180241_i, and clears the flag if now air. It multiplies getPlayerRelativeBlockHardness by elapsed plus one to get fraction f and scales to 0 to 10 stage j. If j differs from durabilityRemainingOnBlock it calls theWorld.sendBlockBreakProgress with player ID, position, and stage, which sends S25PacketBlockBreakAnim to trackers. When f reaches 1.0 it clears the flag and calls tryHarvestBlock to remove and loot the block. Else if isDestroyingBlock is active it performs similar progress for field_180240_f, sending -1 reset and clearing the flag if the block turned to air. Stage caching avoids spamming S25 every tick when unchanged.",
   "code": "    public void updateBlockRemoving()\n    {\n        ++this.curblockDamage;\n\n        if (this.receivedFinishDiggingPacket)\n        {\n            int i = this.curblockDamage - this.initialBlockDamage;\n            Block block = this.theWorld.getBlockState(this.field_180241_i).getBlock();\n\n            if (block.getMaterial() == Material.air)\n            {\n                this.receivedFinishDiggingPacket = false;\n            }\n            else\n            {\n                float f = block.getPlayerRelativeBlockHardness(this.thisPlayerMP, this.thisPlayerMP.worldObj, this.field_180241_i) * (float)(i + 1);\n                int j = (int)(f * 10.0F);\n\n                if (j != this.durabilityRemainingOnBlock)\n                {\n                    this.theWorld.sendBlockBreakProgress(this.thisPlayerMP.getEntityId(), this.field_180241_i, j);\n                    this.durabilityRemainingOnBlock = j;\n                }\n\n                if (f >= 1.0F)\n                {\n                    this.receivedFinishDiggingPacket = false;\n                    this.tryHarvestBlock(this.field_180241_i);\n                }\n            }\n        }\n        else if (this.isDestroyingBlock)\n        {\n            Block block1 = this.theWorld.getBlockState(this.field_180240_f).getBlock();",
   "code_walkthrough": "Incrementing curblockDamage drives both dig paths forward each tick. The receivedFinishDiggingPacket branch finishes blocks whose stop packet arrived before hardness completed. Computing f from hardness times elapsed gives break fraction scaled to S25 stage 0 to 10. sendBlockBreakProgress emits S25 only when the stage changed to limit traffic. Reaching 1.0 harvests the block while the isDestroyingBlock branch maintains live crack for held dig."
  },
  {
   "name": "blockRemoving",
   "detail_long": "blockRemoving handles the stop-digging C07 packet when it arrives mid-break. It first verifies pos equals field_180240_f so stale or moved targets are ignored. It computes elapsed ticks as curblockDamage minus initialDamage and loads the server Block. If material is air it does nothing because onUpdate or harvest already cleared it. It multiplies getPlayerRelativeBlockHardness by elapsed plus one to estimate break fraction f. If f is at least 0.7 it treats the dig as complete: clears isDestroyingBlock, sends progress -1 via sendBlockBreakProgress to erase crack via S25, and calls tryHarvestBlock to remove and drop loot. If below 0.7 and receivedFinishDiggingPacket is not already set, it parks the request by clearing isDestroyingBlock, setting receivedFinishDiggingPacket true, and copying position and initial damage into field_180241_i and initialBlockDamage. updateBlockRemoving will then finish it over subsequent ticks with staged S25 updates.",
   "code": "    public void blockRemoving(BlockPos pos)\n    {\n        if (pos.equals(this.field_180240_f))\n        {\n            int i = this.curblockDamage - this.initialDamage;\n            Block block = this.theWorld.getBlockState(pos).getBlock();\n\n            if (block.getMaterial() != Material.air)\n            {\n                float f = block.getPlayerRelativeBlockHardness(this.thisPlayerMP, this.thisPlayerMP.worldObj, pos) * (float)(i + 1);\n\n                if (f >= 0.7F)\n                {\n                    this.isDestroyingBlock = false;\n                    this.theWorld.sendBlockBreakProgress(this.thisPlayerMP.getEntityId(), pos, -1);\n                    this.tryHarvestBlock(pos);\n                }\n                else if (!this.receivedFinishDiggingPacket)\n                {\n                    this.isDestroyingBlock = false;\n                    this.receivedFinishDiggingPacket = true;\n                    this.field_180241_i = pos;\n                    this.initialBlockDamage = this.initialDamage;\n                }\n            }\n        }\n    }",
   "code_walkthrough": "The pos equality guard ensures only the active dig target can be finished. Elapsed ticks times hardness estimates how far digging progressed. The 0.7 threshold allows slightly early stops to succeed while sending S25 -1 to clear crack. Parking below threshold defers harvest to updateBlockRemoving with staged S25. Air targets are ignored as already broken."
  },
  {
   "name": "cancelDestroyingBlock",
   "detail_long": "cancelDestroyingBlock aborts an in-progress survival dig when the client sends abort or switches target. It unconditionally sets isDestroyingBlock false to stop updateBlockRemoving from advancing crack stages. It calls theWorld.sendBlockBreakProgress with the breaker entity ID, field_180240_f position, and -1 stage. WorldServer forwards that as S25PacketBlockBreakAnim with destroy stage -1 to all clients tracking the position. Receivers clear the crack overlay for that breaker ID. No block removal, loot, or tool damage occurs on cancel. The curblockDamage counter is left for the next onBlockClicked to re-baseline via initialDamage. Duplicate cancels simply resend -1 which clients treat as idempotent clear.",
   "code": "    /**\n     * Stops the block breaking process\n     */\n    public void cancelDestroyingBlock()\n    {\n        this.isDestroyingBlock = false;\n        this.theWorld.sendBlockBreakProgress(this.thisPlayerMP.getEntityId(), this.field_180240_f, -1);\n    }\n\n    /**",
   "code_walkthrough": "Clearing isDestroyingBlock halts tick progress in updateBlockRemoving. Sending break progress -1 emits S25 telling trackers to remove the crack. The breaker ID plus position key the overlay so other diggers are unaffected. No harvest or drops follow because cancellation is visual-only."
  },
  {
   "name": "tryHarvestBlock",
   "detail_long": "tryHarvestBlock removes the block, plays break effects, and corrects the client as needed. It first blocks creative sword use from breaking blocks to preserve attack semantics. It loads IBlockState and TileEntity, then enforces adventure and spectator rules with isAllowEdit and canDestroy checks. It plays AuxSFX 2001 break particles and sound at the position via playAuxSFXAtEntity for nearby clients. It calls removeBlock which fires onBlockHarvested and setBlockToAir plus onBlockDestroyedByPlayer. In creative it sends S23PacketBlockChange with world and position directly to the breaker via playerNetServerHandler so the client matches instant server removal. In survival it damages the held stack, destroys it at zero size, and if both removed and harvestable calls harvestBlock to drop items and XP. The boolean return reports whether setBlockToAir succeeded.",
   "code": "            this.theWorld.playAuxSFXAtEntity(this.thisPlayerMP, 2001, pos, Block.getStateId(iblockstate));\n            boolean flag1 = this.removeBlock(pos);\n\n            if (this.isCreative())\n            {\n                this.thisPlayerMP.playerNetServerHandler.sendPacket(new S23PacketBlockChange(this.theWorld, pos));\n            }\n            else\n            {\n                ItemStack itemstack1 = this.thisPlayerMP.getCurrentEquippedItem();\n                boolean flag = this.thisPlayerMP.canHarvestBlock(iblockstate.getBlock());",
   "code_walkthrough": "Playing AuxSFX 2001 broadcasts break particles and sound to nearby clients. removeBlock performs the actual setBlockToAir with harvest callbacks. The creative branch sends S23PacketBlockChange to the breaker to force client chunk match after instant break. The survival branch inspects the held stack and harvest permission to decide drops and tool damage."
  },
  {
   "name": "tryUseItem",
   "detail_long": "tryUseItem applies right-click item use from NetHandlerPlayServer C08 handling. It immediately returns false for spectator mode to block consumables, throws, and placements. It snapshots stackSize and metadata before calling stack.useItemRightClick with world and player for potions, food, buckets, or projectiles. If the returned stack differs by identity, size, max use duration, or metadata, it writes the result into the current hotbar slot. In creative it restores stackSize and item damage so blocks and items are infinite while still triggering effects. If size reached zero it clears the slot to null to remove consumed items. If the player is not in a use animation it calls sendContainerToPlayer to resync the inventory container, correcting any client-server drift. True signals the interaction was consumed while false lets the caller try block activation instead. No block-change packet is sent here beyond container sync.",
   "code": "    public boolean tryUseItem(EntityPlayer player, World worldIn, ItemStack stack)\n    {\n        if (this.gameType == WorldSettings.GameType.SPECTATOR)\n        {\n            return false;\n        }\n        else\n        {\n            int i = stack.stackSize;\n            int j = stack.getMetadata();\n            ItemStack itemstack = stack.useItemRightClick(worldIn, player);\n\n            if (itemstack != stack || itemstack != null && (itemstack.stackSize != i || itemstack.getMaxItemUseDuration() > 0 || itemstack.getMetadata() != j))\n            {\n                player.inventory.mainInventory[player.inventory.currentItem] = itemstack;\n\n                if (this.isCreative())\n                {\n                    itemstack.stackSize = i;\n\n                    if (itemstack.isItemStackDamageable())\n                    {\n                        itemstack.setItemDamage(j);\n                    }\n                }",
   "code_walkthrough": "The spectator guard blocks all right-click use for observers. Snapshotting size and metadata detects consumption or transformation from useItemRightClick. Writing the result into mainInventory keeps server inventory authoritative. Creative restore plus damage reset preserves infinite items while still firing use effects."
  },
  {
   "name": "activateBlockOrUseItem",
   "detail_long": "activateBlockOrUseItem handles right-click on a block position, choosing block activation versus item placement. Spectators only open chest and inventory GUIs via displayGUIChest without editing, returning false for other blocks. For players it first tries onBlockActivated when not sneaking or when empty-handed, letting doors, chests, furnaces, and levers consume the click with true. If that fails and stack is null it returns false to end the interaction. In creative it records metadata and size, calls stack.onItemUse for placement, then restores damage and size for infinite blocks before returning the result. In survival it directly returns stack.onItemUse which places blocks, consumes items, and triggers TileEntity updates. Block changes from placement flow through WorldServer mark-and-notify as S22 and S23 packets rather than from this method. Deduplication with NetHandlerPlayServer position and face validation prevents ghost blocks.",
   "code": "        else\n        {\n            if (!player.isSneaking() || player.getHeldItem() == null)\n            {\n                IBlockState iblockstate = worldIn.getBlockState(pos);\n\n                if (iblockstate.getBlock().onBlockActivated(worldIn, pos, iblockstate, player, side, offsetX, offsetY, offsetZ))\n                {\n                    return true;\n                }\n            }\n\n            if (stack == null)\n            {\n                return false;\n            }\n            else if (this.isCreative())\n            {\n                int j = stack.getMetadata();\n                int i = stack.stackSize;\n                boolean flag = stack.onItemUse(player, worldIn, pos, side, offsetX, offsetY, offsetZ);\n                stack.setItemDamage(j);\n                stack.stackSize = i;\n                return flag;\n            }\n            else\n            {\n                return stack.onItemUse(player, worldIn, pos, side, offsetX, offsetY, offsetZ);\n            }\n        }\n    }",
   "code_walkthrough": "The sneaking check lets players place blocks instead of opening GUIs when holding items. onBlockActivated gives the target block first chance to consume the click. Null stack returns false after failed activation to stop further work. Creative placement restores metadata and size after onItemUse while survival delegates directly for consumption."
  }
 ]
}